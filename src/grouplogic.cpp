/*
#include "cgrouplogic.h"
void GroupLogic::addConstituentGroup(Group *g) {
    for (int i=0; i<mConstituentGroups.size(); i++) {
      if (mConstituentGroups[i] == g) return;
    }
    mConstituentGroups.push_back(g);
}
*/
